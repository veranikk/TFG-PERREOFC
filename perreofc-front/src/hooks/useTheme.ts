/**
 * Custom React hook for reusable use theme logic.
 * It hides stateful behavior behind a small API that components can consume cleanly.
 */

// Re-exportado desde ThemeProvider para mayor comodidad de importación
export { useTheme } from '../theme/ThemeProvider';
