FROM node:20-alpine

WORKDIR /app

RUN npm install -g pnpm@9.0.0

COPY package.json pnpm-lock.yaml pnpm-workspace.yaml ./
RUN pnpm install --frozen-lockfile

# src/ se monta como volumen en runtime — no se copia aquí

EXPOSE 3000

CMD ["pnpm", "run", "dev"]
